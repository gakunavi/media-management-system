psycopg[binary]==3.2.3
croniter==6.0.0

# ── 既存 .claude/scripts/ が import する外部ライブラリ（§6: 書き直さず呼ぶ）──
#   62本のスクリプトから収集した import のうち、標準ライブラリ以外はこの3つだけ
requests==2.32.3
PyYAML==6.0.2
python-dotenv==1.0.1

# ── GSC API（builtin/gsc_daily.py）──
google-api-python-client==2.149.0
google-auth==2.35.0

# ── GA4 Data API（builtin/ga4_daily.py）──
#   PV とLPファネルを Notion 経由でなく一次ソースから直接取る
google-analytics-data==0.18.11

# ── AIO引用率の計測（builtin/aio_run.py → legacy/aio/aio_monitor.py）──
#   ChatGPT / Gemini に質問して自社が引用されるかを測る。
#   2026-07-23 に Notion から MMS へ移設した際に追加。
openai==2.47.0          # responses API は 1.66 以降。1.59 では動かない（実測）
google-genai==0.8.0
