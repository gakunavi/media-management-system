-- 計測タグの緊急停止を打ち手の型として追加する（§3.10.4 / P2.11）
--
--   過去の TTFB スパイク事故で本当に問題だったのは遅さではなく
--   「何千回発火しても誰も気づかず、止める手段も無かった」こと。
--   発火回数を監視するだけでは足りず、**その場で止められる**必要がある。
--
-- ★他の ActionType と性質が違う（記事を良くする打ち手ではなく、サイトを守る操作）。
--   承認すると受口が受信を止めるので、WordPress 側の作業を待たずに止まる。
ALTER TYPE "ActionType" ADD VALUE IF NOT EXISTS 'stop_tracking_tag';
