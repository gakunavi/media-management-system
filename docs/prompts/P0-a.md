# MMS P0-a — Claude Code 依頼プロンプト

> 使い方: ターミナルで `cd ~/システム開発/Next/media-management-system` → `claude` を起動 → **モデルを Opus に設定** → 下の枠内を丸ごと貼り付け。
> 所要: 約1日（Opus）。出力は6ファイル。

---

## 貼り付けるプロンプト（ここから）

```
あなたはこれから、当社の「メディア／SNS獲得基盤システム（MMS）」の実装仕様を作成します。
これは実装の第0段階（P0-a）であり、コードは書きません。設計書から機械可読な実装仕様を抽出する作業です。

## 入力（唯一の情報源）

`docs/DESIGN.md`
（絶対パス: `/Users/ishiimasataka/システム開発/Next/media-management-system/docs/DESIGN.md`）

- タイトル: 「メディア管理システム（MMS）設計書 v5」
- 2,757行 / `^model ` の出現数 82
- **これ以外の設計書は存在しない。** 見つからない場合は作業を止めて報告すること（推測で別ファイルを使わない）

## ★絶対要件（最重要）

1. **上記設計書を「全文」読むこと。約2,750行あります。部分読み・要約読み・grep だけで済ませることを固く禁じます。**
   この設計書は追記を重ねて作られており、セクション間の整合を確認しながら通しで読まなければ正しい抽出はできません。
2. **設計書に書かれていないことを、推測で補ってはいけません。** 不足・曖昧・矛盾を見つけたら、勝手に解決せず `docs/OPEN-QUESTIONS.md` に記録して報告してください。
3. **設計書の内容を減らしてはいけません。** 「重要度が低そう」という判断で省略しないでください。省略の判断は石井が行います。

## 出力（6ファイル）

出力先ディレクトリ: `/Users/ishiimasataka/システム開発/Next/media-management-system/`（**作成済み・空**）

### 1. `prisma/schema.prisma`
- 設計書 §3 の Prisma ブロック（`// ══ 1.` 〜 `// ══ 10.`）を**完全な Prisma スキーマ**に落とす
- 設計書の記法は簡略化されている（型省略・複数フィールドの1行記述）。**正式な Prisma 構文に展開**する
- 対象は PostgreSQL 16
- 以下を必ず明示する:
  - 全フィールドの型（String / Int / Float / Decimal / DateTime / Boolean / Json / String[]）
  - Nullable（`?`）の判断（設計書で `?` が付いているものは Nullable）
  - enum は Prisma の `enum` として定義（設計書のコメントに `// a | b | c` とある値）
  - リレーション（`@relation`）と外部キー
  - `@@unique` / `@@index`（設計書に明記されているものは必ず。加えて、日次クエリで必要なインデックスを追加し、追加分は `docs/OPEN-QUESTIONS.md` に「設計書に無いが追加した」と明記）
  - `@default` / `@updatedAt`
- **設計書の `model` 定義数と一致すること（82個）**。1つも落とさない
- 各 model の直前に、設計書のどのセクション由来かをコメントで記す（例: `/// §3.6.1 インデックス状況`）

### 2. `docs/PHASES.md`
- 設計書 §9 の実装ロードマップ（59 Phase）を1表に落とす
- 各 Phase について: ID / 名称 / 内容 / 依存Phase / 見積 / **担当モデル（schema.prisma のどの model を触るか）** / **完了条件** / **使用モデル（Opus / Sonnet / Haiku / Fable）**
- 使用モデルは設計書 §9.4 の振り分けに従う
- マイルストーン（M-A 〜 M-E）と、§1.1 の成功条件・撤退条件も転記する

### 3. `docs/RULES.md`
設計書に散在する「実装規約」を1ファイルに集約する。最低限、以下を必ず含めること:
- **計測タグ設計原則7項目**（§3.10.3）— 1ページ1回送信 / スクロールthrottle / 冪等キー / 非同期送信 / WP側で書込みしない / defer / セッション上限
- **欠測とゼロの区別の規約**（§3 `MeasurementCoverage` のコメント）— 行が無い=未計測、0=実測ゼロ、UIで "—(未計測)" と表示
- **provenance / asOf / confidence の扱い**
- **画面パネル番号の定義**（§4.1 の 段1〜段7）
- **WordPress への書き込みは必ず MMS API 経由**（§3.9.1）
- **タイムゾーンは全て JST に正規化**（§16.6 の表 #1）
- **金額は全て税抜で保持**（§16.6 の表 #7）
- **記事本文の正は WordPress、MMS はメタ・計測・バージョン**（§16.6 の表 #8）
- **鮮度3階層ケイデンス**（§7.5.1）と cosmetic 更新の禁止
- 判定ロジック（§5.3）と対照群の最小サンプル基準（§16.5）
- AIに個人情報を渡さない方針（§16.2・氏名/連絡先はマスキング）

### 4. `docs/GLOSSARY.md`
用語と、その**取りうる値の完全なリスト**を定義する:
- `budgetTier` / `funnelStage` / `productFit` / `audience` / `freshnessTier` / `reviewState`
- `Lead.type` / `Lead.status` / `Lead.sourceType`
- `Action.type`（打ち手8タイプ・§5.2）と各々の「効く指標」「判定期間」
- `Idea.source`（5供給源）／`Intervention.verdict`／`TopicCluster.state`／`trafficSource`／`aiEngine`
- `FunnelEvent.step`（7段）

### 5. `docs/check-consistency.sh`
設計書と実装仕様の整合を機械検査するスクリプト。最低限:
- `schema.prisma` の model 数 と 設計書の `^model ` 出現数が一致するか
- 設計書内の Phase 番号に重複が無いか
- 設計書内の「段N」参照が §4.1 の定義（段1〜段7）と矛盾しないか
- ロードマップ表が §9 以外に存在しないか
- `GLOSSARY.md` に定義された enum 値が `schema.prisma` の enum と一致するか
実行して全て pass することを確認してから完了報告すること。

### 6. `docs/OPEN-QUESTIONS.md`
設計書を読んで見つけた**矛盾・曖昧・不足**を全て記録する。各項目に以下を必ず添える:
- 設計書の該当行番号
- 何が矛盾／曖昧／不足しているか
- **なぜ自分では判断できないか**
- **石井に何を決めてほしいか**（選択肢があれば併記）

また、設計書に無いが追加したインデックスも「追加した理由」とともにここに記録する。

> **この設計書は追記を重ねて作られており、矛盾が残っている前提で読むこと。**
> 「矛盾なし」で提出する場合は、その根拠（どう確認したか）を明記すること。

### 7. Git リポジトリの初期化

- このディレクトリで `git init` する（未初期化の場合）
- `.gitignore` は既に存在する（`.env` / DB / backups 等を除外済み）。**内容を確認し、不足があれば追記する**
- 5ファイルの生成完了後、**初回コミット**する
  - コミットメッセージ: `P0-a: 設計書から実装仕様を抽出（schema.prisma / PHASES / RULES / GLOSSARY / check-consistency）`
- リモートリポジトリの設定は不要（ローカルのみ。石井が後で判断する）
- **`.env` や認証情報を絶対にコミットしない**（`git status` で確認してからコミットすること）

## 禁止事項

- 設計書を部分的にしか読まずに作業を始めること
- 設計書に無い機能・モデル・フィールドを勝手に追加すること（インデックスを除く。追加時は `docs/OPEN-QUESTIONS.md` に明記）
- 設計書の矛盾を自己判断で解決すること（報告する）
- 「重要度が低い」と判断して省略すること
- この段階でアプリケーションコードを書くこと（P0 の作業）

## 完了報告に必ず含めること

1. 設計書を全文読んだことの確認（総行数・最終セクション名）
2. 出力した6ファイルのパスと行数
3. `check-consistency.sh` の実行結果（全項目 pass）
4. **`docs/OPEN-QUESTIONS.md` の内容**: 設計書内で見つけた矛盾・曖昧・不足の一覧（各項目に「設計書の該当行番号」と「なぜ判断できないか」と「石井に何を決めてほしいか」を添える）
5. 設計書に無いが追加したインデックスの一覧と、その理由
6. `git log --oneline` と `git status` の結果（初回コミットが済み、機密ファイルが含まれていないこと）

まず設計書を全文読み込んでください。読み終えたら、着手前に「読み取った全体像」を10行以内で要約して提示し、私（石井）の確認を待ってください。
```

## 貼り付けるプロンプト（ここまで）

---

## 石井さんへの補足

### 事前に決めておくこと（プロンプト内で聞かれます）

| 項目 | 推奨 | 備考 |
|---|---|---|
| リポジトリの場所 | `~/システム開発/Next/media-management-system/` | 変えたい場合はプロンプト内の出力先を書き換える |
| Git管理 | **する** → **Claude Code に任せる**（プロンプトに指示済み） | 設計変更の履歴が残る |
| AIへの個人情報 | **マスキングする** | プロンプトに反映済み |

### この作業で期待すること

- P0-a の目的は「**設計書2,750行を、実装で使う700行程度の正確な仕様に圧縮すること**」
- 圧縮であって省略ではない。**1対1対応を保つ**
- 以降の Phase は、この6ファイルだけを読めば実装できる状態になる

### 完了後の次の一手

`P0`（Docker Compose + Next.js 15 + Prisma + Auth.js + launchd）。
モデルは **Fable 5 または Opus**（ファイル数が多く往復が増えるため）。

### 注意

**`docs/OPEN-QUESTIONS.md` が空で返ってきたら疑ってください。**
この設計書は追記を重ねて作ったため、矛盾や曖昧さが残っている可能性が高いです。
何も出てこない場合は、全文を読んでいない可能性があります。
