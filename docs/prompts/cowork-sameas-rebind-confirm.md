# cowork への返信: 必須確認 — その理解で合っています（実データで確認済み）

そのまま cowork に貼って使うプロンプト。

---

## ★必須確認への回答: **はい、合っています**

推測ではなく、**公開記事100本の本文をWP APIで取得して数えました**。

```
  調べた記事: 100 本
  ★本文に生 lin.ee が残っている: 0 本    ← ご指摘のとおり
  本文に /r/line/ がある        : 100 本
  両方ある                      : 0 本
```

**生 `lin.ee` は1本も残っていません。**
したがって修正版の href 限定 inject を今のDB本文に当てても
**置換対象ゼロ → 何もしない**で、汚染された `sameAs` は残ったままです。

**既存分は `organization.yaml` から JSON-LD を作り直す（re-bind）が必須**、
という理解で正しいです。点2の「再生成」はその意味です。

---

## 任意1（ドライラン）: **やります**

ご指摘のとおりです。前回公開時からテンプレートやスクリプトが変わっていれば、
`sameAs` 以外の差分が混ざります。**1本で再生成 → レンダリング結果を diff →
「変わったのは sameAs だけ」を確認**してから残りをバッチに乗せてください。

★こちらからのお願い: **diff の結果を先に見せていただけますか。**
`sameAs` 以外に差分があった場合、それが意図した改善なのか事故なのかは
そちらでないと判断できません。こちらは「本文が変わったこと」しか分かりません。

## 任意2（単引用符）: **いまは不要と確認しました**

`bind-template.py` に `href='`（単引用符）の出力は **0箇所**でした。
現状は二重引用符固定なので取りこぼしません。

ただし**将来の保険としては入れておく価値がある**と考えます。
今回の事故そのものが「いまの入力なら大丈夫」が崩れた形なので、
判断はお任せします（対応するなら両対応、しないなら現状維持で問題ありません）。

---

## ★点3の先出し: sameAs が無い記事について（重要な発見）

「無いのが正か」を調べたところ、**無いのは正しくありません**でした。
しかも前回お伝えした5本より多く、**100本中14本**ありました。

```
  sameAs 無し: 14 本
   うち Organization スキーマを出している: 14 本   ← ★全部
   うち JSON-LD 自体を出している        : 14 本
```

**14本すべてが Organization スキーマを出しているのに `sameAs` だけ欠けています。**
＝ 入るべきなのに入っていない状態です。

### 共通点（slug と公開日で切り分けました）

```
  速報系っぽい slug（flash / 末尾 YYYY-MM）: 6本 → sameAs あり 0本 / 無し 6本
  それ以外                                : 94本 → sameAs あり 86本 / 無し 8本

  公開日の範囲: 2026-05-06 〜 2026-06-07 の14本に集中
```

例:
```
  keieikyoka-fy2026-reform-flash
  shogaku-genka-shokyaku-40man-shikko-flash-2026-05
  aircraft-operating-lease-detailed-2026-05
  jigyo-shokei-tokurei-keikaku-extension-2026-05
  keieikyoka-fy2026-reform-detailed-2026-05
  ai-zeimu-chosa-ksk2-2026
```

**速報系（flash）は6本すべてが `sameAs` 無し**で、公開日も一定期間に固まっています。

★こちらからは「別の生成経路（news-factory 系？）を通っている」か
「`organization.yaml` に `same_as` を足す前に公開された」かの
**どちらかまでしか絞れません**。当時の経緯はそちらの方が分かるはずです。

**ご確認いただきたいこと**: この14本は
- (a) 別パイプラインで `sameAs` を出していないだけ → **re-bind すれば入る**
- (b) 意図的に出していない → **現状維持**

どちらでしょうか。(a) なら **25本ではなく 39本**（86 + 14 のうち該当分）が
再生成の対象になります。★**対象本数が変わるので、バッチ前に確定させたい**です。

---

## 進め方（合意した内容の確認）

| 点 | 内容 | 状態 |
|---|---|---|
| 1 | 差分適用（href 限定） | ✅ 承認いただきました |
| 2 | 既存分は **re-bind**（re-inject ではない） | ✅ 理解一致・実データで裏付け済み |
| 2' | **1本ドライラン → diff 確認 → バッチ** | ✅ 実施。**diff をこちらにも共有ください** |
| 2'' | 通常の publish パイプライン経由（直接 REST パッチはしない） | ✅ §8-1 に沿います |
| 3 | 再生成後に MMS 側で一括検査 | ✅ やります（下記） |
| ★ | **sameAs 無し14本の扱いを (a)/(b) で確定** | ⏳ **返答待ち** |

### 点3でこちらが報告すること

- 全記事の `sameAs` が `https://lin.ee/szd8e1x` になっているかの突合
- `sameAs` が無い記事の残数と、その内訳（Organization スキーマの有無つき）
- ★**リダイレクタが残っている記事が0本になったか**

---

## 予防策（`url_health.py` の JSON-LD 内URL検査）

ご指定の2点、そのまま入れます。

- **(a)** JSON-LD 内の全URL（`sameAs` / `BreadcrumbList.item` / `url` / `logo` 等）を
  抽出して**実際に叩く**
- **(b)** **301 の先まで辿って最終ステータス**を見る
  （パンくず404がまさに「301 → 404」で、1ホップ目だけ見ていると気づけない形でした）

実装して動かしたら結果をご報告します。

★**パンくず404（`/category/capital-investment/` → 301 → 404）は
検出の話とは別に、生きているSEOバグとして個別に直す前提**、承知しました。
別チケットとして `docs/prompts/cowork-breadcrumb-404.md` で先にお送りしています。
そちらの対応もお待ちしています。
