# MMS 実装規約（RULES）

> 抽出元: `docs/DESIGN.md` に散在する実装規約を1ファイルに集約したもの。
> **★全Phase がこのファイルを読む。** 設計書本文を読み直す必要はない。
>
> 各規約の末尾に設計書の由来セクションを記す。**規約に反する実装はレビューで差し戻す。**

---

## 0. 最上位の原則

| # | 原則 | 由来 |
|---|---|---|
| **0-1** | **正確さ > 効率。** 効率化は、正確さを落とさない方法でのみ行う | §9.4.5 |
| **0-2** | **モデル定義の正は `prisma/schema.prisma` のみ。** 設計書 §3 を実装の入力に直接使わない | §9.4.5 |
| **0-3** | **ロードマップの正は `docs/PHASES.md` のみ**（設計書側は §9 のみ） | §9 冒頭 |
| **0-4** | **画面パネル番号の正は本ファイル §4**（設計書 §4.1） | 冒頭 L24 |
| **0-5** | **1セッション = 1Phase。** 節約が目的ではなく、混線による事故を防ぐのが目的 | §9.4.5 |
| **0-6** | **設計書に無いことを推測で補わない。** 不明点は `docs/PHASES.md` §8（未解決リスト）に追記して報告する | P0-a 絶対要件2 |

---

## 1. ★計測タグの設計原則（7項目・絶対）

> 由来: §3.10.3。**過去にサイト重量化事故（TTFBスパイク）を起こしている。**
> 原因は WPテーマの自前PV計測が**同期でDB書込み**していたこと。
> **本当に問題だったのは「何千回発火しても誰も気づかなかったこと」。同じ設計にしない。**

| # | 規約 |
|---|---|
| **① 1ページあたりの送信は原則1回** | イベントは `sessionStorage` にバッファし、`beforeunload` / `visibilitychange` で **`sendBeacon` 1発**でまとめてPOSTする |
| **② スクロールは throttle 250ms** | 深度は **25 / 50 / 75 / 100% の4段のみ**。**連続値を送らない**（スクロールは1操作で数百回発火しうる） |
| **③ 冪等キー** | `(sessionId, step, contentItemId, 分単位のtimestamp)`。**重複は受信側で捨てる** |
| **④ 送信は必ず非同期** | `sendBeacon` / `fetch keepalive`。**同期XHR禁止** |
| **⑤ WordPress 側では一切DB書込みをしない** | 受けるのは MMS の `/api/ingest`。**`post_meta` / `wp_options` を太らせない** |
| **⑥ 計測JSは defer / 遅延読込** | **`document.write` 禁止** |
| **⑦ 1セッションあたりのイベント上限を JS 側にも置く** | 例: 50件。超えたら送信を止める（**暴走の自己遮断**） |

### 1.1 サーバー側の二重の歯止め

- `/api/ingest` に**レート制限**を置く（同一セッションから毎分N件超は **429**）
- `TelemetryVolume` で**発火回数そのものを監視**する（§3.10.4）

| 閾値（初期値・調整可） | アクション |
|---|---|
| `eventsPerSession > 30` | 段7に**黄**（想定は7〜15） |
| `eventsPerSession > 50` | 段7に**赤** ＋ 即通知 |
| 前日比でイベント総数が **3倍** | **赤 ＋ 計測タグの自動無効化を提案**（段5にワンクリック停止Action） |
| `rejectedDuplicates` 比率 > 20% | 実装バグの疑い。**黄** |

### 1.2 その他の負荷関連の規約

| 対象 | 規約 | 由来 |
|---|---|---|
| A/B の群割当 | **サーバー側で決定する**（JS描画だとちらつき＝CLS悪化） | §3.10.2 |
| Microsoft Clarity | 外部JS。**非同期・遅延読込**にする | §3.10.2 |
| `LinkCheck` | **週次・レート制限つき**（自社サイトへの定期リクエスト） | §3.10.2 |
| `UptimeCheck` | **軽量 HEAD** リクエスト | §3.10.2 |
| WP書き込み（P1.8） | **バッチ化・レート制限**する | §3.10.2 |
| 計測タグの設置場所 | **子テーマ or 専用プラグイン化。`functions.php` 直書き禁止**（テーマ更新で消える） | §16.6-5 |

### 1.3 デプロイ前の性能ゲート（§3.10.5）

- WPテーマ・計測タグ・LPを更新する**前後で必ず PageSpeed Insights を測る**（`PerfGate`）
- **TTFB が 20% 以上悪化、または LCP が 0.5秒以上悪化したらデプロイを失敗扱い**にして段7に赤
- **ロールバック手順を必ず併記**する（`ContentVersion` / テーマzipの旧版）

> **過去の事故はテーマ更新に紛れて入った。更新のたびに測れば、次は当日中に気づく。**

---

## 2. ★★ 欠測とゼロの区別（絶対・最重要）

> 由来: §3 `MeasurementCoverage` のコメント。
> **この規約を破った結果、「問い合わせ 0」と表示して直客2件の成約を見逃した。**

```
MeasurementCoverage に行が存在しない  = 「未計測」
value = 0                            = 「実測ゼロ」
```

| # | 規約 |
|---|---|
| **2-1** | **UI/API は「未計測」を `—(未計測)` と表示し、決してゼロと書かない** |
| **2-2** | `MeasurementCoverage` に期間が無い指標は、**集計・グラフ・brief から physically 除外する**（0として集計に混ぜない） |
| **2-3** | 計測を開始したら必ず `MeasurementCoverage` に `startedAt` を記録する。**記録を忘れると、その指標は永久に「未計測」扱いになる** |
| **2-4** | MCP の `mms_acquisition_brief` は **`coverage` と `dataQuality` を先頭に置く**。AIは必ずこの2つを先に読んでから結論を出す（§15.2） |

### 2.1 欠測の検知（§3.2.2）

| 規約 | 内容 |
|---|---|
| **日次を永久保持** | `ContentMetric` / `KeywordRanking` に日次で入れ、**削除しない** |
| **★GSC APIは16ヶ月しか遡れない** | それ以前はGoogleから取得不能。**自前DBに貯め続けることだけが唯一の長期履歴**。日次ジョブを止めない運用が資産価値そのもの |
| **欠測の自動補填（backfill）** | 日次ジョブは「最終取得日〜昨日」の**欠けている日を毎回チェックして埋める**。1日失敗しても翌日に自動回復する |
| **欠測アラート** | **3日以上の欠測を段7で赤表示**（過去に10日欠測を誰も検知できなかった） |

---

## 3. provenance / asOf / confidence の扱い

> 由来: §3 `UnitEconomics.provenance` / §3.4.4 `AdSimulation.assumptionSource` / §3.8.3 / §16.5 / §15.2

### 3.1 provenance（数値の出所）

| 値 | 意味 | 扱い |
|---|---|---|
| `measured` | **自社の実測値** | そのまま使える |
| `declared` | **人の申告値**（例: 電話問い合わせの手動記録・`Lead.sourceType=phone_manual`） | 実測と混ぜて集計しない。UIで区別する |
| `estimated` | **推定値／ベンチマーク**（例: LP CVR の業界一般値・検索ボリューム） | **絶対値を信じず、相対比較と推移で使う** |

| # | 規約 |
|---|---|
| **3-1** | **`AdSimulation` は各前提が実測かベンチマークかを `assumptionSource` に必ず明記する**。LP CVR は自社実測が無いため、**初回は必ず `estimated`** |
| **3-2** | 検索ボリュームは推定値（Keyword Plannerの丸め・各ツールの推定）。**絶対値を信じない**（§3.3.1） |
| **3-3** | 電話問い合わせの手動記録は `provenance=declared` として扱う（§3.8.3） |

### 3.2 asOf（いつ時点のデータか）

| # | 規約 |
|---|---|
| **3-4** | 全ての集計値・グラフに「**いつ時点のデータか**」を併記する |
| **3-5** | 鮮度の管理対象: `KeywordResearch.expiresAt`（ラッコ90日）／`ContentItem.nextReviewDue`（§7.5）／`SnsAccountHealth.tokenExpiresAt`（Threads 60日） |
| **3-6** | **GSCは反映遅延2〜3日ある**。「最新日＝昨日」を前提にしない |

### 3.3 confidence（判定の信頼度）

> 由来: §16.5。記事157本では層別すると対照群が数本しか取れず、統計的に判定できない打ち手がある。

| # | 規約 |
|---|---|
| **3-7** | **対照群の最小サンプル基準: 5記事以上 かつ 合計impressions 500以上**。満たさなければ `verdict = inconclusive` |
| **3-8** | **`inconclusive` は「効果なし（neutral）」とは別物**として UI に明示する。混同を許さない |
| **3-9** | **バッチ判定（最有効）**: 同型の打ち手を複数記事にまとめて適用し、`Intervention.batchId` 単位で判定してサンプル数を稼ぐ |
| **3-10** | 対照群が作れない場合はサイト全体トレンドで補正し、**`confidence = low` を付与**する |
| **3-11** | 判定期間の逐次延長: **14日 → 28日 → 56日**。それでも不足なら `inconclusive` で確定 |

---

## 4. 画面パネル番号の定義（★これが正）

> 由来: §4.1。**設計書 §17 に段6/段7 を取り違えた記述があるが、本表が正**（`docs/PHASES.md` §8 U22）。

| 段 | 名称 | 内容 |
|---|---|---|
| **段1** | **結果** | **問い合わせ件数（ゴール）と受け皿別の内訳**。目標に対する進捗・前期間比・信号（2026-07-22 改訂） |
| **段2** | **ファネル（経路の階段）** | 表示 → クリック → CTA表示 → CTAクリック → LP到達 → LP内CTA → 送信。**最大ドロップ地点を明示** |
| **段3** | **買い手の質** | 買い手適合クリック（`budgetTier=high/mid`）／比較段階（`funnelStage=comparison`）の流入／買い手PVあたり問い合わせ率 |
| **段4** | **今週の変化** | operator が**日本語3〜5行**で生成（履歴保存） |
| **段5** | **次の一手** | `[承認][却下][差戻し]`。押すと Server Action で即 jobs 投入。**`impacts` の重み順にソート** |
| **段6** | **施策の生死** | 撤退期限までのカウントダウン |
| **段7** | **ジョブ健全性** | 最終実行・失敗・APIトークン残日数・**計測の欠測アラート** |

| # | 規約 |
|---|---|
| **4-1** | **段番号はこの7つのみ。** 段8以降を作らない |
| **4-2** | 段1に「未計測」を表示する場合は `—(未計測)` とし、0と書かない（§2-1） |
| **4-3** | 段5に並べる `Action` は `impacts` が空なら**起票時点でAPIが弾く**（§5） |
| **4-4** | **段1のゴールは「問い合わせ件数」。LINE登録（`Lead.type=line_friend`）を合算しない。** 登録は受け皿への到達であって相談ではない。合算すると商談が増えていないのにゴールに近づいたように見える |
| **4-5** | **段1の内訳は受け皿（`Lead.sourceType`）で出す。** `Lead.type`（直客/代理店）はゴールの種類であって経路ではなく、それだけでは何を直すか決まらない（打ち手は受け皿ごとに違う） |
| **4-6** | **最大ドロップは、転換率を算出できた区間が2つ以上あるときだけ名指しする**（`lib/stages.ts` の `buildFlow()`）。区間が1つなら、その区間が必ず最大になるので情報がゼロの上に誤誘導する（§16.5） |
| **4-7** | **画面の集計期間は1か所（`lib/period.ts` の `resolveRange()`）で決め、全ての集計に同じ `since`/`until` を渡す。** データ源ごとに「最終取得日から28日」を取ると、同じ画面に別々の28日間が並ぶ（実際に GSC 7/20基準と GA4 7/11基準が1本のファネルに並んでいた） |
| **4-8** | **月次目標（`Target`）と比べるのは、表示期間がちょうど1暦月のときだけ**（`Range.period !== null`）。任意の30日と月次目標を比べると達成/未達の判定が嘘になる |
| **4-9** | **分母が割れない軸で階段を割らない。** 公式LINEの登録には種別（見込み客／代理店見込み）が無く、分かるのは問い合わせ以降。階段を種別で割ると分子だけ割れて率が壊れる。内訳は問い合わせ以降の表で見る |
| **4-10** | **反映待ちと欠測を分ける。** GSC は2〜3日遅れて入るのが正常。末尾の未到着分を「欠測」と書くと毎日警告が出て、本物の欠測が埋もれる |
| **4-11** | **Threads のゴールは並列に2つ（①メディア送客 ②DM）。** 縦に積んで1本の階段にしない。DM狙いの投稿が「送客していない」と評価され、逆も起きる。狙いの判定は**貼ったリンク**（自己申告の列を作ると、実際に貼ったものとズレたときに嘘が残る） |
| **4-12** | **倍率（×n）の基準はその表自身の中央値。** 母集団の違う表に同じ分母を使わない（ターゲット別にフォーマット別の中央値を使い「代理店候補 ×0.1」と出していた） |
| **4-13** | **「未計測」「実測ゼロ」「結果待ち」を3つとも区別する**（`Stage.pendingLabel`）。選別中のDMを未計測と書くと計装の不備に見え、0 と書くと失敗に見える。打ち手がそれぞれ違う |
| **4-14** | **計測系が違う数字を同じ階段に載せない**（2026-07-23 石井さん）。公式LINEは「送客クリック → 登録」を1本の階段にしていたが、クリックと登録は別の計測系で**同じ人だと確認する手段が無い**（follow に経路情報が入らない）。率を出しても意味が無く「送ったのに登録されない」という誤読を生む。送客は別枠（入口）に出し、**階段は登録から始める** |
| **4-15** | **`@db.Date` 列のフィルタには `Range.dateWindow` を使う**（`lib/period.ts`）。Prisma は日付列のフィルタ値を**日付に切り捨てる**ため、JSTの0時（＝UTC前日15:00）を渡すと前日として扱われ、**期間の頭に1日多く入り、末尾の当日が丸ごと落ちる**。実測で「今月」の clicks に 6/30 が混入していた。対象は `MetricSnapshot` / `ContentMetric` / `AgencyLpDaily` / `SnsAccountHealth` / `KeywordRanking` / `ClusterMetric` / `SerpSnapshot` / `AdMetricDaily`。**タイムスタンプ列（`occurredAt` / `publishedAt` / `addedAt` …）には使わない** |
| **4-16** | **記事の分類は「誰に向けて・何を書いたか」だけ持つ**（`ContentAudience` / `ContentFormat`）。**記事から読者の予算規模は決まらない**ため `budgetTier` を記事に付けてはいけない。同じ外貨両替機（350万/台）の記事を1台買う人も数千万分買う人も読む。実際、唯一の成約（480万＝定義上「中」）の記事を「高」と分類していた（**実測1件が分類を否定した**）。金額はリード側（`Lead.budgetTier`）で商談時に聞いて入れる |
| **4-17** | **タグ別の実績は「1記事あたり」を主に見る**（`tagStats()`）。記事数が違うタグを実数で比べると本数の多いタグが必ず勝ち、「増やすべき型」を取り違える。法人向け133本 vs 両方11本で、実数は法人が勝つが1記事あたりは両方（5.4）が上 |
| **4-18** | **計測タグは `data-mms` 属性に依存しない**（`mms-tag.js`）。属性を貼った要素しか見ない作りだったため、記事159本のどこにも `data-mms` が無く、**受け口はあるのにイベントが1件も来ていなかった**（実測は `lp_scroll` 1件のみ）。159本に属性を貼らせるのは現実的でなく、貼り漏れも見えない。記事には既に `data-article` があるので、タグ側で `a[href]` を自動で拾う |
| **4-19** | **計測タグから来た `meta` を生で jsonb に保存しない**（`sanitizeLinkMeta()`）。キーを固定し・長さを切り・語彙外を落とす。行の肥大と個人情報の混入経路を同時に塞ぐ。**クエリ文字列は落とす**（UTM や個人を含みうる） |
| **4-20** | **`new URL(href, ベースURL)` でスキーム無しの href を正規化しない**。計測タグは既に `host + pathname` に削って送るため、ベース付きで解釈すると相対パス扱いになり `collect.asset-support.co.jp/r/line/x` が `example.invalid/collect.asset-support.co.jp/r/line/x` に化ける（2026-07-23 の投入テストで検出）。**スキーム付きのときだけ URL として解釈する** |
| **4-21** | **リダイレクタの実測と計測タグの実測を同じ数字として並べない**。リダイレクタ（`/r/`）はサーバー側なので広告ブロックの影響を受けず、計測タグは JS が動いた分だけ。**合計はリダイレクタが正**、内訳（記事別・リンク別）は計測タグで見る、と用途を分けて画面に明記する |
| **4-22** | **記事のメインKWは実測（GSC page×query）から決める**。人が「狙っていたKW」を入力すると、狙いと実測のズレが消えてリライト対象が見つからなくなる。狙いは `Keyword`（ラッコの調査KW）、実測は `ContentQuery`。**両者はふつう一致しない**（そのズレ自体が打ち手） |
| **4-23** | **根拠が薄いものをメインKWにしない**（`MIN_CLICKS=1` / `MIN_IMPRESSIONS=10`）。最初の試行では、クリック0・表示1回のノイズ（「"設立 2026年"」）が記事のメインKWになっていた。表示1回は「その記事が何で戦っているか」の証拠にならない。落とした件数は必ず表に出す |
| **4-24** | **公的機関を名指しした検索語（「国税庁 …」）を「押されていない＝タイトルが悪い」と読ませない**。実測で低CTR上位はほぼこの型（3.9〜6.6位・計445表示・0クリック）で、利用者は公式ページを開きに来ている。**直してもクリックは増えない**。`isNavigational()` で印を付け、直せるものを上に出す |
| **4-25** | **`ContentQuery.position` は期間平均なので合算しない**（GSCの定義）。日次に落とすと行数が跳ね、GSC側も閾値で行を落とすため、**期間の集計スナップショット**として週次で持つ |
| **4-26** | **人が決めた値がある項目を実測で上書きしない**（2026-07-23 の訂正）。メインKWとクラスタを「未管理」と判断して GSC 実測から埋めたが誤りで、`art-kw-map.yaml` / `media.db` / `pillar-plan.md` に人が決めた値が既にあった。実測を正にすると**狙いと実測のズレが消え、リライト対象が見つからなくなる**。狙い＝`mainKeywordId`/`KeywordAssignment(main)`、実測＝`ContentQuery` の2本立てにする。**空欄を見たらまず「本当に未管理か」を疑う** |
| **4-27** | **日本語名から slug を作るときは必ず名前のハッシュを付ける**。ASCII化だけだと別物が同じ slug に潰れる（「事業承継・退職金 Cluster」も「事例・インタビュー Cluster」も `cluster`）。`ON CONFLICT (slug) DO UPDATE` と組み合わさり、**17クラスタが黙って14に減り、所属記事が別のクラスタに付いた**。件数を数えて初めて気づいた |
| **4-28** | **未来日付を `lastReviewedAt` に入れない**。台帳の「最終更新日」に本日より後の日付が3件あった（公開予定日と混在していた）。未来日を基準にすると `nextReviewDue` も未来になり、**その記事は永久に見直し督促されない**。除外して件数を出す |
| **4-29** | **同じKWを狙う記事が複数あるとき、`KeywordAssignment(main)` の一意制約で片方を黙って落とさない**。`ContentItem.mainKeywordId` は**全記事に**入れて狙いを残し、`main` は実測クリックの多い1本に付ける。落ちた側は画面にカニバリとして出す（実測7語18本が該当） |
| **4-30** | **「ピラーが無い」を1種類として扱わない**。P1〜P3は紐付けが外れていただけ（実在した）、横串2つは**設計上そもそも置かない**（クラスタ横断でPillar-Cluster構造を取らない）。`state` だけでは区別できないので `TopicCluster.note` に理由を残す。区別しないと**直す必要の無いものを直そうとする** |
| **4-31** | **リスク記事(C柱)を「商用60〜90日」の鮮度に入れない**（cowork 実運用・2026-07-23）。C柱は制度・構造の解説で時事性が低い。実測でも intervention 9件中**リスク記事は0件**＝公開後に一度もリライトされていない。`reference`（12ヶ月）＋法改正トリガが正しい |
| **4-32** | **鮮度の期限だけでリライト督促を出さない**（二段構え）。処理能力は**週2〜3本**で、期限切れを全部赤にすると件数が能力を超えた瞬間に誰も見なくなる。期限は境界に留め、実運用の着手理由で最多の **CTR不全（順位10〜25位・表示あり・クリック0）** と重なった記事を先に出す。※"順位が落ちた"ではなく**"順位は取れているのにクリック0"**が主トリガ |
| **4-33** | **リライトの中身を「タイトルにKWを足す」にしない**。実測では効いていない（ART-061: 24.7位→10.3位に改善したがクリックは0のまま、その後21位へ再下落）。効いたのは**読者意図に合わせた即答型タイトル/メタ**（ART-101: クリック4→7・表示203→509）。順位10位外ならタイトル単独では不十分で本文加筆（H2/共起語）が要る |
| **4-34** | **`FreshnessTier` と `AioTier` を混同しない**。AioTier(hot/warm/cold/none) は「AI検索での引用を**どの頻度で計測するか**」、FreshnessTier(breaking/commercial/evergreen/reference) は「記事を**どの間隔で見直すか**」。同じ「Tier」でも測っている対象が違う |
| **4-35** | **リライト督促から「実着手できない記事」を除く**（cowork 指摘・2026-07-23）。① 統合済み/301済み（URLを持たないプレースホルダ＝直す実体が無い。実測で ART-006 が毎回上位に出ていた） ② 効果測定の待ち中。これを入れないと督促の上位が「直せない記事」で埋まる |
| **4-36** | **「リライト直後か」を `lastReviewedAt` で判定しない**。「cosmetic更新は逆効果・実質追記のときだけ `dateModified` を更新」が既存ルールなので、**タイトル/メタだけ直した記事は最終更新日が動かない**。まさに我々が推奨している直し方がそれ。実際 ART-058 は 7/3 リライト済なのに最終更新日は 4/23 のままだった。**`Intervention.evaluateAt`（実施日＋28日）を使う** |
| **4-37** | **督促の根拠に指名検索（「国税庁 …」）を使わない**。§4-24 で `/keywords` は下へ回していたのに、リライト督促側では拾っていた（不整合）。指名検索しか根拠が無い記事は督促に出さない。出すと「タイトルを直せ」という**効かない指示**になる |
| **4-38** | **督促に固定の文面を出さない。原因で打ち手を出し分ける**（cowork 指摘・2026-07-23）。同じ「表示はあるのにクリック0」でも、**同じ語で他の記事も表示されていれば原因はカニバリ**で、タイトルを直しても互いに食い合ったまま。実測では残った督促3本が**全部カニバリ**（ART-008は自クラスタの ART-040 が6位で上、GPU系は3記事が13〜21位に散在）で、既定文面のままなら3本とも効かない指示になっていた。`CtrFailure.rivals` の有無で分岐する |
| **4-39** | **301元を「競合記事」として集計に混ぜない**（`ContentItem.redirectsToId`）。同じ内容が複数URLに残るのは常態で、2026-07-23 だけで3件踏んだ（ART-006→ART-142／ART-105→ART-189／chushokigyo-…→ART-142）。記録しないと**実体の無いURLがカニバリ・CTR不全・競合一覧に並ぶ**。実際「即時償却」のカニバリ12記事に301元が2本入っていた（除外後10記事・表示186→137） |
| **4-40** | **URLは「MMSに登録されている」だけでなく `curl` で挙動を確かめる**。ART-142（P1ピラー）は本番で **301の無限ループ**に陥っており、読者もクローラも到達できなかった（表示246・クリック0）。cowork の「統合は順位改善に効いていない」という評価も、実際は**統合の設定ミスで記事が消えていた**のが原因だった。★推測でURLを組み立てて検証しない（一度 404 のURLで誤った不具合報告をしかけた） |
| **4-41** | **`link_click` の href から `#断片` を落とさない**。目次リンクは断片が無いと全部同じ href になり、「どの見出しへ飛んだか」が消える（実測で最初に届いた5件のうち2件が目次クリックだった）。落とすのは**クエリだけ**（UTM や個人を含みうる） |
| **4-42** | **台帳の突合と実URLの生存確認は別物。両方回す**。2026-07-23 のピラー301ループは**台帳・config・MMS の三者が一致していた**ため静的な突合では一切引っかからなかった。逆に slug の打ち間違いは叩く前に静的で分かる。`ledger_check.py`（静的・原因）＋ `url_health.py`（動的・症状）の2段構え |
| **4-43** | **URL健全性チェックの対象から「異常が正常なもの」を外す**。301元（301を返すのが正常）・下書き（公開していないので404が正常）・プレビューURL（`?post_type=`）・URL欄が壊れた行。外さないと毎日「異常」が並び、**本物の異常が埋もれて誰も見なくなる** |
| **4-44** | **静的キャッシュを迂回して叩く**。Xserver の静的キャッシュが古い301を返し続けることがあり、実際それがループの一因だった。クエリを1つ足して素通りさせる（記録は元URLで残す） |
| **4-45** | **URLチェックで「直すべきもの」と「経年で消えた旧URL」を分ける**。`article`（現役記事）の404・全件のループ・多段は**障害**。`article_unlinked` / `site_page` の404は実測があるだけの旧URLで**正常**。分けないと毎日同じ25件が並び、本物が埋もれる（実測: 26件 → 要対応0件） |
| **4-46** | **日本語パスのURLは percent-encode してから叩く**。しないと `urllib` が `UnicodeEncodeError` で落ち、`/サービス/` のような**実在するページを「取得失敗」と誤判定**する。ホストは IDNA |
| **4-47** | **「一度も動いていない」を異常として出さない**。週次ジョブ3本が実行0回だったが、登録が火曜・木曜で**最初の月曜がまだ来ていない**だけだった。赤で出すと意味のない警告を毎日見ることになり、本物の停止が埋もれる。**「初回待ち」と「予定を過ぎたのに動いていない」を分ける** |
| **4-48** | **未実行ジョブの遅延起点は `createdAt`**。最終実行が無いときに遡り続けると「予定29回分の遅れ」という嘘が出る（実測で出た）。起点は「最終実行、無ければ登録日」 |
| **4-49** | **停止中のジョブに理由を書く**（`Job.note`）。`aio-*` 3本は OpenAI のクォータ枯渇で意図的に止めているが、画面には巨大なスタックトレースしか出ておらず「直すべき障害」に見えていた。理由が無いと誰かが再有効化して同じ失敗を繰り返す |
| ~~**4-50**~~ | ~~Job/JobRun への DDL は worker を止めてから当てる~~ → **§4-51 で根治したため不要** |
| **4-51** | **DB接続を開いたまま外部I/O（HTTP・sleep）をしない。長く待つ処理では `autocommit=True` にする**。psycopg は既定で最初の SELECT からトランザクションを開き、閉じるまで `idle in transaction` が残る。実害は2つ: ①**DDL がロック待ちで固まる**（`ALTER TABLE "Job"` が4分以上ブロックし後続クエリが全部詰まった。修正後は0.3秒） ②**autovacuum が回収できずテーブルが肥大する**（ディスクは一度満杯にして Postgres を落としている・§11.9）。該当は `worker.py`（20秒sleep）／`url_health.py`（235回のHTTP・数分）／`line_followers.py`（最大400回のAPI）。★autocommit 下でも `conn.commit()` は例外を出さない（検証済み） |
| **4-52** | **worker の再起動で打ち切られた実行を `failed` にしない**（`aborted`）。デプロイのたびに実行中のジョブが「失敗」として積み上がり、失敗件数が信用できなくなる。実際 `url-health-daily` は直前が success なのに、イメージを作り直したせいで「失敗」と表示されていた。**打ち切りと失敗は別物**で、打ち切りは次回の予定で再実行されるので直す必要が無い |
| **4-53** | **異常の件数だけを出さない。「動いている数」を必ず並べる**。異常だけだと 0/21 でも 20/21 でも見た目が同じで、全体の何本が生きているのか分からない |
| **4-54** | **画面に開発用語を出さない**。「ジョブ」→「自動処理」、`gsc-fetch-daily` の隣に「検索の実測を毎日取り込む」を置く。名前だけでは**止まったときの重大さが判断できない** |
| **4-55** | **止めている理由は「なぜ止めたか」だけでなく「止めている間できないこと」と「再開手順」まで書く**。理由だけだと再開してよいのか誰も判断できない。例（AIO 3本）: OpenAI のクレジット追加を止めているため毎回429で失敗する ／ その間 AI検索での被引用（実測3.6%）が測れない ／ 再開は課金 → `MMS_AIO_ENABLED=1` → `npm run seed:jobs` |
| **4-56** | **コスト画面には「払っているもの」を全部載せる。載っていない支出は無いのと同じ**。実測で OpenAI（AIO 3ジョブの停止原因）と Cloudflare（¥909/月）が**どちらも未登録**だった。前者は当日ずっと議論していた対象、後者は石井さんの指摘で発覚。★「月額合計 ¥0」を「無料で運用している」と読ませない（前払い/従量は別枠と明記する） |
| **4-57** | **残高は金額ではなく「あと何回動くか」で出す**。`残高 $0.1372` では多いか少ないか判断できない。実際は次回の実行に $0.24 かかるため**途中で止まる**（あと0.57回分）。閾値も金額固定（旧実装は0.3USD）にせず、**次回の実行ぶんに足りるか**で判定し、**止まる処理の名前**まで出す |
| **4-58** | **「判定期日超過 0」を「問題なし」と読ませない**。実際は**1件も期日を決めていないから0**だった（6件中6件が未設定）。未設定件数を別に出す |
| **4-59** | **エッジキャッシュの正体を推測で書かない**。`url_health.py` に「Xserver の静的キャッシュ」と書いていたが、実測すると **Cloudflare APO**（`server: cloudflare` / `cf-edge-cache: cache,platform=wordpress`）だった。APO は**HTMLをエッジでキャッシュする**ため、WordPress 側で301を直しても古い応答が返り続ける。クエリを足すと迂回できる（`cf-apo-via` が `origin,nohtml` → `origin,qs` に変わるのを実測で確認） |
| **4-60** | **無料のサービスもコスト画面に載せる**。「無料だから載せない」と、**止まったときに誰も気づけない**。実測で LINE公式アカウント（段1の受け皿そのもの）・GA4（PVの唯一の取得元）・GAS（4処理が依存）・エックスサーバー（サイト本体）が**全部未登録**だった。★載せる基準は「払っているか」ではなく**「止まると何かが測れなくなるか」** |
| **4-61** | **使っているサービスはジョブから逆算して洗い出す**。手で思い出すと必ず漏れる（実測で4件漏れていた）。日次ジョブの一覧と `.env` の外部サービス設定を突き合わせれば、依存は機械的に出る |
| **4-62** | **無料枠には「いつ有料になるか」を書く**。LINE公式アカウントは月200通まで無料で、友だちが増えると従量（ライト月5,500円〜）に切り替わる。いま7人なので無料枠内だが、**増えることが目標**なので必ず超える |
| **4-63** | **専門用語を画面に出したら、その場で意味を書く**。「判定期日」だけでは何の日か分からない（石井さんから「これ何」）。→「見直し予定日」に変え、**続けるか止めるかを決める日**であること、決めないと払い続けることを画面に明記した |
| **4-64** | **自社が元々持っているもの（`shared`）を「無料」と一緒にしない**。エックスサーバーは無料ではなく**会社が既に払っている**もので、メディアのための追加コストが無いだけ（2026-07-24 石井さん）。コスパを判定するのは**追加コスト**であって総支出ではない。ただし**止まればサイトごと落ちる**ので、依存関係としては一覧に残す |
| **4-65** | **実績（月次記録）が残っているツールを削除させない**。消すと推移から過去の月まで消え、「先月は安かった」という嘘の推移になる。使わなくなったものは **`state=stopped`（停止）**にする。それが正しい記録。誤登録をすぐ消せる道は実績が無いものにだけ残す |
| **4-66** | **固定費と変動費を混ぜて1つの数字にしない**（2026-07-24 石井さん指摘）。合計だけでは「**使っていないのに高い**」と「**使った結果高い**」が区別できず、打ち手が正反対になる（前者は止める、後者は成果次第）。推移は積み上げで色を分け、合計を出すときも必ず内訳を併記する |
| **4-67** | **前払い/従量の変動費は「残高の減り」から積む**。契約上の月額が存在しないため。補充（残高が増える）ときは消費0として残高だけ更新する。★円換算の為替は仮定なので **原単位（$）を必ず併記し、レートも画面に出す**。1つの数字に丸めると後から検証できない |
| **4-68** | **合計の推移は月をクリックして内訳を開けるようにする**。「今月 ¥963」だけでは何に払ったか分からず、止める判断ができない。★0円のもの（無料・自社既存）は内訳に出さない（埋まって読めなくなる） |
| **4-69** | **ダッシュボードに「1件あたりの獲得コスト」を出さない**。ツール費 ¥909 ÷ 問い合わせ4件 = ¥227 は**人件費・外注費を含まない**ので、獲得単価として読むと桁が2つ違う判断になる。出すのは「いくら払っているか」「増えていないか」「止まりそうか」の3つ |
| **4-70** | **一覧の絞り込みには件数を必ず出す**。0件であること自体が情報になる（例: 「変動 0件」なら使った分の費用が発生していないと分かる）。件数が無いと、押してみるまで何があるか分からない |
| **4-71** | **課金形態で絞り込めるようにする**。固定と変動は**打ち手が違う**（固定は「止めれば必ず減る」、変動は「使わなければ減る」）。混ざったままだとどちらの手を打つべきか判断できない |
| **4-72** | **「判定できるか」を判定日より前に出す**。判定は「適用後28日 − 適用前28日 − 対照群」で出すが、**実測が無ければ何をしても判定不能**にしかならない。しかもそれが分かるのは判定日当日で、その時点では打つ手が無い。実測の**日数**を前後で出し、0日のものは事前に警告する（実測: pending 7件中3件が判定不能確定だった） |
| **4-73** | **実測の「0日」を「値が0」と読ませない**。ART-090 は後28日の実測が0日だが、これは「クリックが0だった」ではなく「**検索結果に一度も出ず測れていない**」。前者は打ち手の失敗、後者は測定の失敗で、対処が違う |
| **4-74** | **前提が壊れていた打ち手は判定日を延ばす**。そのまま判定すると**嘘の学習**が残る。実測: ART-142 は統合の効果を測るはずが 301ループで記事に到達できず検索から消えていた。これを「効果なし」と判定すると「統合は効かない」という誤った学習になる。★**延期には理由を必須にし、履歴を画面に出す**。理由なく延ばせると都合の悪い判定を永久に先送りできる |
| **4-75** | **提案の並び順を `impacts.length`（説明文に書いた指標の数）で決めない**。「効きそうか」ではなく「何個書いたか」で並んでいた。**取り逃している表示回数**で並べる（順位もCTRも表示が母数なので、表示が少ないものは直しても動く余地が無い） |
| **4-76** | **判定待ちの記事に重ねて提案しない**。同じ記事に続けて手を入れると**どちらの効果か分からなくなる**。実測で3件が該当し、うち ART-101 は進行中と**同じ `title-meta-rewrite`** を重ねて提案していた。判定日まで後ろへ回す |
| **4-77** | **指名検索が主の記事を「CTR異常」として提案しない**（§4-24 の立案側への反映）。「表示は多いのにクリック0」という信号だけ見ると必ず上位に来るが、**タイトルを直しても増えない**。実測: ART-060 は表示151のうち「国税庁 …」型が61%で全部クリック0、これが最優先で出ていた。表示の50%以上が指名検索なら後ろへ回す |
| **4-78** | **処理能力を超える件数を同じ重みで並べない**。承認待ち23件に対し実際に回せるのは週2〜3本。全部並べると上から見て力尽き、一覧そのものが見られなくなる。**上位N件だけ出し、残りは畳む**。★件数と「なぜ後ろへ回したか」は必ず表示する（黙って隠すと「無い」と読まれる） |
| **4-79** | **打ち手の優先順位を「重み係数」で決めず「枠」で分ける**（cowork 依頼7回答・2026-07-24）。**表示回数の単一ソートは新規記事を構造的に最下位へ沈める** — 自社がSERPに不在のKWは GSC の表示がほぼ0なので、「取り逃している表示」では新規記事の価値が**原理的に測れない**。月10枠を**リライト6・新規4**に分ける（新規1:リライト2が目安。リライトは効果1〜3ヶ月・工数小、新規は6〜12ヶ月・工数大。新規を0にするとSERP空白KWを永久に取り逃す） |
| **4-80** | **内部リンクと Threads を記事の枠と同じ土俵で競わせない**。1件15〜30分の軽作業で、記事1本ぶんの工数がかからない。枠外にして随時実行する。★実測で内部リンク提案が1件しか出ていなかったのは過小（内部リンクの69%がクラスター↔クラスター、ピラーへは19%＝権威が集約されていない） |
| **4-81** | **新規記事に「根拠が弱い（母数が小さい）」を適用しない**。SERPに居ないので表示が少ないのは当たり前で、それを弱い根拠と呼ぶと新規が永久に選ばれない。**その少ない表示こそが「Googleが関連と認識している」証拠**であり、需要の裏付けになる |
| **4-82** | **検索語の内訳が取れていない記事に `title_meta_rewrite` を出さない**（cowork 依頼7回答）。実測: ART-072 は**ページ表示131に対しクエリ内訳が0行**、ART-179 は表示166に対し3。GSC は表示の少ないクエリを伏せるため、**極端に細かいクエリに散っている**とこうなる（cowork の言う AIモード合成クエリ汚染の典型症状。ART-061 は表示503の95%が合成で人間クエリの実順位は23.2位だった）。★「合成クエリだから」と断定はしない。確実なのは「**どのクエリに向けて直せばよいか分からない**」ことで、それだけで前提が崩れる |

---

## 5. 結果フォーカスの構造的強制

> 由来: §5.5

| # | 規約 |
|---|---|
| **5-1** | **全 `ContentItem` / `Action` / `Idea` に `impacts`（効く結果指標）が必須。** どの結果指標にも効かない作業は**登録できない**（APIで弾く） |
| **5-2** | 段5は `impacts` の重み順にソートする |
| **5-3** | 月次で「今月の作業のうち結果指標に効いた割合」を自動算出する |
| **5-4** | `ActionEvent.reason`（却下理由）を蓄積し、**次回の立案プロンプトに直近の却下理由を注入**する。3週連続で却下率が高い打ち手タイプは**自動停止**（§5.6） |

---

## 6. 判定ロジック（Check）— PDCAが閉じる場所

> 由来: §5.3

```
netEffect = (適用後28日の実測) − (適用前28日の実測) − (対照群の同期間トレンド)

対照群 = 同カテゴリ・同 funnelStage で、その期間に手を入れていない記事群
```

| verdict | 条件 | 自動アクション |
|---|---|---|
| `positive` | netEffect > 期待効果の下限 | `Learning` 生成 → **同型の打ち手を他記事へ横展開提案** |
| `neutral` | 有意差なし | `Learning` 生成 → 立案の重みを下げる |
| `negative` | 悪化 | `Learning` 生成 → **ロールバック提案** ＋ 同型の打ち手を一時停止 |
| `inconclusive` | サンプル不足 | 判定期間を延長（§3-11） |

| # | 規約 |
|---|---|
| **6-1** | **対照群補正（`controlDelta`）を必ず入れる。** 無いと「季節変動で上がっただけ」を「施策が効いた」と誤判定する |
| **6-2** | **季節性の補正も併用する。** 全記事が同じ季節性を持つ場合、対照群補正は効かない（treatment も control も同時に下がる）。`SeasonalityIndex` で調整し、**段1・段4に季節調整値を併記**する（§3.8.1） |
| **6-3** | **ロールバック先を必ず作る。** `Intervention` の前後に `ContentVersion` を保存する。戻す先が無いと negative 判定に対応できない（§16.3） |
| **6-4** | 記事単位では母数不足（クリック142/週 ÷ 157本 = 週1件未満）。**クラスタ単位／記事群単位で判定する**（§3.5.3） |

### 6.1 母数に応じた検証手法の使い分け（§3.7.0）

> **今の段階で小さなCTA文言のA/Bを回すのは時間の無駄。** LPの構成を大きく変えて before/after で見る方が正しい。

| 母数の段階 | 使う手法 |
|---|---|
| **今（LP訪問 〜100/月・実測 約38/月）** | ① **定性**（Clarity録画・リードへのヒアリング）② **大きく変えて before/after** ③ **クラスタ／記事群単位の判定** |
| 中（100〜1,000/月） | ④ **SEOスプリットテスト**（`SplitTest`・主判定手法）＋ ②③ 継続 |
| 先（1,000+/月） | ⑤ **LP/CTA の A/Bテスト**（`Experimentation`） |

| # | 規約 |
|---|---|
| **6-5** | **SEOスプリットテストは層別ランダム化する**（クラスタ × 順位帯 × funnelStage を揃えてから振り分ける）。無作為に分けると初期条件がズレて結論が歪む |
| **6-6** | **LP/CTA A/B は開始時に必要サンプル数を自動計算し、到達見込みが3ヶ月を超えるなら `underpowered` として起動を拒否する。** その場合は「大きく変えて before/after」を `Intervention` 経由で提案する |
| **6-7** | **Clarity は MMS に統合しない**（設計判断）。`Clarity = 探索（仮説を見つける）` / `MMS = 検証（仮説を定量で確かめる）`。無理に取り込むと保守負担だけ増える |

---

## 7. ★鮮度3階層ケイデンスと cosmetic 更新の禁止

> 由来: §7.5.1（`content-standards.md` が正典）

| freshnessTier | 対象 | 間隔 | 例 |
|---|---|---|---|
| `breaking` | 速報・税制改正 | **随時**（法改正イベント駆動・intervalDays=0） | ニュース記事・改正大綱 |
| `commercial` | 商用・税制系 | **60〜90日（既定75日）** | 即時償却・経営強化税制・商材比較 |
| `evergreen` | エバーグリーン・Pillar | **6ヶ月（180日）** | 節税の基本ガイド |
| `reference` | 定義・リファレンス | **12ヶ月（365日）** | 用語解説 |

### 7.1 ★cosmetic 更新の禁止（絶対）

| # | 規約 |
|---|---|
| **7-1** | **`dataUpdatedAt`（dateModified）は「実質追記」のときだけ更新する。** cosmetic な変更では絶対に触らない |
| **7-2** | `ArticleReview.outcome = no_change` のときは `dataUpdatedAt` を**更新しない**。`updatedDataUpdatedAt = false` を記録する |
| **7-3** | 記事に **`last reviewed: YYYY-MM`** を明示表示する（`lastReviewedAt`） |
| **7-4** | YMYL記事は **`infoBaseDate`（情報基準日）を記事内に表示**する（必須） |

> **cosmetic更新は逆効果。** 中身が変わっていないのに更新日だけ動かすことを禁じる。

### 7.2 自動化（§7.5.2）

```
nextReviewDue = lastReviewedAt + FreshnessRule.intervalDays

日次ジョブ:
  nextReviewDue − 30日 → reviewState = due_soon → 段1に黄色
  nextReviewDue 経過   → reviewState = overdue  → 段1に赤 ＋ 段5に Action 自動起票
```

**期限以外のトリガー（イベント駆動）**

| トリガー | 検知元 | 起票される Action |
|---|---|---|
| 期限到来 | `nextReviewDue` | `periodic_review` |
| **順位下落**（4週で3位以上低下） | `KeywordRanking` | `triggered_by_rank` |
| **表示はあるがクリック0**（CTR異常） | `ContentMetric` | `title_meta_rewrite` |
| **税制改正・法令変更** | News monitor / `RegulatoryEvent` | `triggered_by_law`（**該当する全記事を一括で対象化**） |
| **競合に抜かれた** | `SerpSnapshot` | `triggered_by_competitor` |
| **KW鮮度切れ**（ラッコ90日） | `KeywordResearch.expiresAt` | `kw_refetch` |
| **AIO非引用**（Hot tierで連続hit無し） | AIO計測 | `geo_reinforce` |

### 7.3 レビューの手順（§7.5.3）

1. 記事内の制度・数値・年度を抽出
2. **一次ソース（国税庁・e-Gov・財務省）と自動照合**
3. 差異があれば `findings` に記録し、修正draftを生成
4. 石井さんは段5で**承認するだけ**
5. `outcome` を記録（`no_change` なら `dataUpdatedAt` は更新しない）
6. 実質追記なら `Intervention` に接続して効果測定へ

---

## 8. WordPress との関係

### 8.1 ★書き込みは必ず MMS API を経由する（§3.9.1）

```
★ WordPress への書き込みは、必ず MMS API を経由する
   メディア事業部の既存スクリプトも MMS 経由に切り替える
   （wp-publish.py → /api/wp/publish）
   → 書き込み経路が1本になり、原理的にズレない
```

| # | 規約 |
|---|---|
| **8-1** | **WP への直接書き込みを禁じる。** 別セッションのAIが叩く `wp-publish.py` や緊急 surgical fix（live post への REST PATCH）も MMS 経由に切り替える |
| **8-2** | それでも残る緊急PATCH用の保険として、**日次で WP から `title / metaDescription / status / tags / featured_media` を取得しハッシュ比較**する（本文全体は比較しない＝軽量） |
| **8-3** | 差分検出時のみ段7に表示し、`ContentVersion` を1件作って取り込む |

### 8.2 ★記事本文の正は WordPress（§16.6-8）

| # | 規約 |
|---|---|
| **8-4** | **本文の正は WordPress。MMS が持つのはメタ・計測・バージョン。** 二重管理しない |
| **8-5** | `ContentVersion.bodyHtml` は**ロールバック用のスナップショット**であり、編集の正ではない |

---

## 9. タイムゾーン（★全て JST に正規化）

> 由来: §16.6-1。**日付境界がズレて集計が合わなくなる典型。**

| # | 規約 |
|---|---|
| **9-1** | **全ての日時を JST に正規化して保存する** |
| **9-2** | **GSC は PT 基準**。PT → JST の変換ルールを実装し明文化する |
| **9-3** | GA4 はプロパティ設定に依存する。取り込み時に JST へ揃える |
| **9-4** | `date` 型（`@db.Date`）の「1日」は **JST の 00:00〜23:59** とする |

---

## 10. 金額（★全て税抜で保持）

> 由来: §16.6-7

| # | 規約 |
|---|---|
| **10-1** | **金額は全て税抜で保持する。表示時に「税抜」と注記する** |
| **10-2** | 型は `Decimal @db.Decimal(14, 2)`。Float を使わない |

### 10.1 ★ユニットエコノミクスの実数（2026-07-20 石井確定）

> 主力商材は **ML（マイグレーションライト）** に固定。

| 項目 | 値 |
|---|---|
| 1台の販売価格 | **240万円（税抜）** |
| 当社取り分（総枠） | **20% ＝ 48万円 / 台** |
| 代理店手数料（新規契約のベース） | **7.5% ＝ 18万円 / 台** |
| **① 直客の粗利** | **48万円 / 台** |
| **② 代理店経由の粗利** | **30万円 / 台（12.5%）** |
| **③ 直客プレミアム** | **18万円 / 台** |
| 実績（初の直客） | 1問い合わせ → **2台成約 → 粗利 96万円** |

```
上限CPA = 1成約あたり粗利 × 問い合わせ→成約率
```

| # | 規約 |
|---|---|
| **10-3** | **`UnitEconomics` は `acquisitionChannel`（direct / agency）別に必ず分ける。** 粗利が違う |
| **10-4** | **直客1件は代理店経由1.6件分の価値**（48万 ÷ 30万）。投資評価は**直客ベース（粗利48万）で行う** |
| **10-5** | **問い合わせ→成約率は未実測**（実績は1問い合わせ→1成約だが母数1で使用不可）。実務では**保守側（成約率10〜20%・1台想定）＝上限CPA 5〜10万円**を初期の判断線に置く |
| **10-6** | **小額テスト（例: 10万円 / 2〜3週間）を飛ばして本番予算を投じない。** LP CVR の実測が無い状態のシミュレーションは、前提が1つズレると結論が10倍変わる |

---

## 11. 個人情報・AIへの受け渡し

> 由来: §16.2・§19.0（2026-07-20 石井確定）

| # | 規約 |
|---|---|
| **11-1** | **★AIに個人情報を渡さない。** 返信ドラフト生成では**氏名・連絡先をマスキング**し、**興味商材と閲覧履歴のみ**を渡す |
| **11-2** | **Clarity のフォーム入力はマスキングする。** 全フォーム項目に `data-clarity-mask` を付与し、氏名・電話・メールが録画に残らないようにする（§3.7.3） |
| **11-3** | `Lead` の個人情報カラムは**列単位で暗号化**する。アクセスは `AuditLog` に記録する |
| **11-4** | **第三者提供の同意チェックボックスは実装しない**（ML事業部として当社で対応しており第三者提供に該当しないため・石井確定）。ただし `ConsentRecord` モデルは**将来、代理店へリードを配分する運用に変えた場合のために残す** |
| **11-5** | **保存期間を守り、期限到来で自動削除する**（`DataRetentionPolicy`）: `VisitorSession` = **13ヶ月** / `FunnelEvent` = **25ヶ月** / `Lead` の個人情報 = 取引終了後 n年 |
| **11-6** | プライバシーポリシーに**取得情報・利用目的・第三者提供先・保存期間・開示請求窓口**を明記する（現行ポリシーの改定が必要） |
| **11-7** | 電気通信事業法の**外部送信規律**により、外部送信する場合は通知・公表が必要。自前1st-partyのみでもポリシーへ記載する |
| **11-8** | **個人情報の列は `apps/web/lib/crypto.ts` の `encryptPii()` を通してのみ保存する**（AES-256-GCM・封筒形式 `v1:iv:tag:ct`）。対象: `Lead.contactName` / `.contactEmail` / `.contactPhone` / `.companyName` / `.note`（P2-D18） |
| **11-9** | **`MMS_PII_KEY` は fail-closed。** 未設定なら暗号化できないので `/api/ingest/*` は **503 を返して受信を拒否**する。「鍵が無いから平文保存」を構造的に禁じる |
| **11-10** | **`MMS_PII_KEY` を失うと既存の個人情報は復号できない。** バックアップ対象（§16.4）。鍵ローテーションは再暗号化スクリプトとセットで行う |
| **11-11** | 復号 `decryptPii()` の結果を**ログ・通知・AI 受け渡しにそのまま渡さない**。必ず `maskContact()` を通す（§11-1） |

> ✅ 専門家確認は**行わない**（2026-07-20 石井確定・`docs/PHASES.md` §9-D14）。ポリシー改定自体は P0.5 で実施する。

---

## 11.5 計測受口（`/api/ingest/*`）の規約

> 由来: §8（HMAC）／§16.1-④（冪等キー）／§3.10.4（レート制限）。実装は `apps/web/lib/{hmac,rate-limit}.ts`。

| # | 規約 |
|---|---|
| **I-1** | **全受口が HMAC-SHA256 署名検証を持つ。** 署名対象は `"{timestamp}.{生のボディ}"`。ボディを再エンコードしてから署名しない |
| **I-2** | **timestamp を署名に含め、±300秒外は 401**（リプレイ攻撃対策） |
| **I-3** | **冪等キーで二重起票を防ぐ。** Lead は内容から主キーを導出し、再送で増えない（§16.1-④） |
| **I-4** | **レート制限を持つ**（既定 1分30件・`MMS_INGEST_RATE_LIMIT`）。超過は 429 + `Retry-After`。JS 側の自己遮断（§1-⑦）と二重の歯止め |
| **I-5** | **署名シークレット未設定は fail-closed**（503）。「検証スキップ」にしない |
| **I-6** | 受口は `middleware.ts` の公開パス（署名が認証を兼ねるので Auth 不要） |
| **I-7** | **接続を開始したら必ず `MeasurementCoverage` に記録する**（§2-3）。逆に、まだ繋いでいない受口の計測記録を作らない（「0」と誤表示され事故が再発する） |

---

## 11.9 ★ストレージの空きを監視する（2026-07-23 追加）

**2026-07-23 にディスクが99%まで埋まり、Postgres がチェックポイントを書けず
クラッシュループした。**

```
PANIC: could not write to file "pg_logical/replorigin_checkpoint.tmp": No space left on device
→ checkpointer 強制終了 → 自動再起動 → また満杯 → 無限ループ
```

| # | 規約 |
|---|---|
| **11.9-1** | **ジョブの成否ではストレージ枯渇に気づけない。** DBが落ちればジョブ自体が動けず、失敗の記録すら残らない。段7に**独立した監視項目**として持つ（`getStorageHealth()`） |
| **11.9-2** | 閾値は**残り容量ではなく使用率**で持つ（90%で警告・95%で赤）。ディスク総容量は環境で違い、残りGBで決めると環境ごとに閾値を変えることになる |
| **11.9-3** | **掃除を人手に頼らない。** `scripts/docker-gc.sh` を日次 04:30（`com.mms.docker-gc`）で回す。日次ジョブ（05:00〜）より前に済ませ、ジョブが動く時点で空きを確保する |
| **11.9-4** | **消してよいのはタグ無しイメージとビルドキャッシュだけ。** `docker image prune` に `-a` を付けない（停止中コンテナのイメージまで消え、起動できなくなる）。`docker volume prune` は**絶対に使わない**（DBのデータが消える） |

---

## 12. セキュリティ

### 12.0 ★認可は `lib/session.ts` の `currentUser()` / `isOwner()` を通す（2026-07-23）

`auth()` を Server Action から直接呼ばない。

- 本番は `AUTH_URL` が https のため、Auth.js は Cookie 名を
  `__Secure-authjs.session-token` として読む
- localhost の自動ログイン（`/api/dev-login`）は http なので、ブラウザが
  `__Secure-` 付き Cookie を受け付けず、素の名前で発行している
- 結果 **localhost では `auth()` が常に null を返し、owner 限定の Server Action が
  全て「権限がありません」で落ちる**（リード手動登録・LP台帳登録・ジョブ実行）。
  画面は開けてしまうため、誰も気づかないまま「登録できない」だけが残る

`currentUser()` は auth() を試したうえで、**localhost かつ自動ログインが有効なときだけ**
素の Cookie を Session テーブルに突き合わせる（/api/dev-login と同じ三重ガード）。
本番の Cookie 設定（Secure 属性）は変えない。

> 由来: §8

| # | 規約 |
|---|---|
| **12-1** | Next.js は **127.0.0.1 バインド**。外部公開は **Cloudflare Tunnel + Access（メール認証）のみ**。**素のトンネル公開はしない** |
| **12-2** | Webhook 受口は **HMAC-SHA256 署名検証**（共有シークレット）。WPプラグイン / GAS に同鍵 |
| **12-3** | 認証は Auth.js（Email magic link）。**Role（owner / partner / readonly）を最初から持たせる** |
| **12-4** | シークレットは `.env`（**コミット禁止**）。環境変数接頭辞は `MMS_*` |
| **12-5** | Postgres は**日次論理バックアップ**（`pg_dump` 30世代）＋**週次で Google Drive へ退避**（暗号化） |
| **12-6** | **投稿の分離ガード**: `ContentItem` は必ず `channelId` を持つ。**channelId 無しの投稿は API レベルで拒否**する。worker は `channelId` 経由でしかトークンを引けない（§11.3） |

### 12.1 障害復旧（§16.4）

| 項目 | 定義 |
|---|---|
| **RPO**（許容データ損失） | **24時間** |
| **RTO**（復旧目標時間） | **4時間** |
| バックアップ3箇所 | ① ローカル `backups/` 30世代 ② Time Machine / 外付けSSD ③ **Google Drive（暗号化・週次）** |
| 復旧手順書 | リポジトリに **`RECOVERY.md`** を置く |
| **リストア訓練** | **四半期に1回、実際に別環境で復元して起動確認**。手順書だけでは復旧できない |
| 可視化 | 段7に「最終バックアップ日時」「**最終リストア検証日**」を表示 |

---

## 13. 計測の正しさをどう担保するか

> 由来: §16.1。**計測が誤れば、その上の分析・判定・戦略・広告投資が全部無意味になる。**

| # | 対策 | 規約 |
|---|---|---|
| **①** | **合成モニタリング（★これだけは必ず入れる）** | **日次で Playwright が自社サイトを実際に巡回**（記事 → CTA → LP → フォーム表示）し、**期待する `FunnelEvent` がDBに記録されたか検証**する。不一致で段7を赤にする |
| **②** | **二重計測の突合** | GSCクリック vs 自前セッション数、GA4 PV vs 自前PV の**乖離率を日次監視**。**±15%超で警告**。★**どちらが正しいかは判断せず「ズレている」ことを検知する** |
| **③** | **ボット除外** | UA・IP・行動パターン（滞在0秒・step順序が不正）でフィルタ。**除外率そのものも監視**（急増＝実装異常のサイン） |
| **④** | **冪等キーで重複排除** | `(sessionId, step, contentItemId, occurredAt秒)` でユニーク制約。**二重計測を物理的に防ぐ** |
| **⑤** | **外れ値検知** | 前日比・4週平均比で10倍／1/10 等を検出。**欠測検知とは別軸**（「ゼロになった」だけでなく「異常に増えた」も拾う） |
| **⑥** | **集計ロジックの単体テスト** | ゴールデンデータ（既知の入力→既知の出力）で集計関数をテストする |

---

## 14. アトリビューション

> 由来: §16.6-2・§3.6.2

| # | 規約 |
|---|---|
| **14-1** | **first touch / last touch の両方を保持し、主指標は last touch** |
| **14-2** | **Cookie 有効期間は 90日** |
| **14-3** | **クロスデバイスは追わない（割り切る）** |
| **14-4** | **アシスト貢献（`LeadTouchpoint`）を必ず併記する。** first/last だけだと間の記事が評価ゼロになり、"効いていない記事" として畳まれる危険がある |
| **14-5** | 配分方式は線形 or 時間減衰（`attributionWeight`）。**片方だけ見ない** |

---

## 15. 自動化してよいもの／人の承認を残すもの

> 由来: §12.3。**設計思想: 「AIが判断材料を完成させ、人は3秒で押す」。**
> 石井さんの作業時間はほぼゼロになるが、**責任の所在は人に残す。**

| 対象 | 判定 | 残す理由 |
|---|---|---|
| **WP公開の最終切替** | 🔷 **人が押す** | YMYL（税務）。誤情報の公開は税理士法・景表法リスクに直結し取り返しがつかない。**バリデータ全通過＋AI照合表付きで「読んで押すだけ」まで削るが、押すのは人** |
| **一次情報ファクトチェックの承認** | 🔷 **人が押す** | AIが照合表を作ることと、その表が正しいと判断することは別 |
| **DM/リプの個別税務相談・批判への返信** | 🔷 **人が押す** | 税理士法（個別税務助言の禁止）＋レピュテーション |
| **CAPTCHA** | 🔸 **AI突破は禁止** | Bot検知ポリシー違反。**アカウントBANは事業の即死**。検出時のみ通知し、それ以外は自動 |
| **撤退・大型投資の判断** | 🔷 **人が押す** | 経営判断そのもの |
| `BrandMention` の negative | 🔷 **通知のみ** | **AIは自動返信しない**（§3.9.5） |

| # | 規約 |
|---|---|
| **15-1** | 上記以外の手動ステップは**完全自動化する**（§12.2 の14ステップ判定表） |
| **15-2** | **リマインダー方式（「石井さんがローカルで実行してください」）を新規に作らない。** MMS は常駐サーバーなので時間制限・DB書込制限・状態消失が全て解消される |
| **15-3** | `Action.expiresAt` は **14日**。超えたら自動 `expired` にし、段1に「未処理n件」を表示する（§16.6-3） |

---

## 16. 外部サービス・API の扱い

| 対象 | 規約 | 由来 |
|---|---|---|
| **note** | **公式APIが無い。非公式APIのスクレイピングは採用しない**（規約グレー・2026年に2度塞がれた・BANは事業の即死）。`Channel(type=note)` は**手動入力チャネル**として定義し、未入力なら段1の信号を黄色にする | §11.2 |
| **AIO計測** | 自前スクレイピングは OpenAI ToS 上グレー。**頻度を抑え Hot tier のみ**。Otterly（$29/月・MCP対応）への置換パスを常に持つ | §16.6-6 |
| **DataForSEO** | SERP は $0.60 / 1,000 SERP。追跡300KW × 週次 ≒ **$0.72/月**。日次でも約$5/月 | §3.3.5 |
| **Threads トークン** | 期限60日。**自動リフレッシュ＋残日数を段7に常時表示（9日前から黄）** | §17・§3.8.5 |
| **既存Python資産** | **書き直さない。** worker の `legacy/` に置き**そのまま呼ぶ**。中身に依存しない（疎結合） | §6・§9.4.5 |
| **Threads GAS** | **継続。** Insights を `/api/ingest/threads` へPOSTするよう1関数追加するだけ | §6 |
| **Notion** | **廃止。** ただし**移行後もDBはアーカイブとして残す（削除しない）** | §7.5 |
| **Google/Yahoo 広告** | ⚠️ 節税商材が「金融サービス」と判定されると審査が厳しい。**広告文にも tax-accuracy ルール（「必ず節税できる」等の断定表現の禁止）を適用**する。不承認が続く可能性を小額テストで先に確認する | §3.4.6 |

---

## 17. AI（MCP）からの読み書き

> 由来: §15

| # | 規約 |
|---|---|
| **17-1** | **書き込みは `mms_propose_action` に限定する。** AIが数値やコンテンツを勝手に書き換えられない設計にする |
| **17-2** | `mms_acquisition_brief` を読むときは**必ず `coverage` と `dataQuality` を先に見る**。「未計測」を「0」と読んで結論を出さないため |
| **17-3** | MCPツール接頭辞は `mms_*`、環境変数接頭辞は `MMS_*`、DB名は `mms` |

---

## 18. 拡張時の規約（壊れずに強化するために）

> 由来: §10。**汎用モデルに寄せてあるので、追加は「データが増える」であって「コードが増える」ではない。**

| 拡張シナリオ | 必要な作業 | 禁止事項 |
|---|---|---|
| Instagram を追加 | `Channel` を1行INSERT ＋ ingest 用 Route Handler 1本 | **スキーマ変更しない** |
| 新しい指標（AI引用hit率・リール指標など）を追加 | `MetricSnapshot.metric` / `ContentMetric.metric` に値が増えるだけ | **マイグレーションしない** |
| 新規事業を追加 | `Business` を1行INSERT | — |
| 新しい打ち手の型を追加 | operator の立案ロジックに1関数（＋ `ActionType` enum に1値） | **UIは変更しない**（`Action` を表示するだけ） |
| パートナーに一部を見せる | Auth.js の Role を付与 | **権限モデルを作り直さない** |

---

## 19. 変更管理

| # | 規約 |
|---|---|
| **19-1** | **スキーマを変更したら `docs/GLOSSARY.md` の enum 値も同時に更新する**（`check-consistency.sh` が検査） |
| **19-2** | **Phase を追加・変更したら `docs/PHASES.md` のみを直す。** 設計書 §9 以外にロードマップ表を作らない |
| **19-3** | **設計書（`docs/DESIGN.md`）を変更したら `docs/check-consistency.sh` を実行する** |
| **19-4** | 設計書と抽出物（本ファイル群）が食い違ったら、**抽出物を勝手に直さず `docs/PHASES.md` §8 に記録して報告する** |

---

## 20. P0-a の決定に伴う実装規約（→ [docs/PHASES.md](docs/PHASES.md) §9 決定記録）

| # | 規約 | 由来 |
|---|---|---|
| **20-1** | **Prisma は 6 系に固定する**（`prisma@^6` / `@prisma/client@^6`）。7 系は `datasource.url` を廃止し `prisma.config.ts` へ移すため、P0 の基盤立ち上げと同時に踏まない | D7 |
| **20-2** | **メインKWの正は `KeywordAssignment(role=main)`。** `ContentItem.mainKeywordId` は読み取り用の非正規化キャッシュ。**書き込みは必ず `KeywordAssignment` を更新してから `mainKeywordId` を同期する。** 逆順・片側だけの更新を禁じる | D6 |
| **20-3** | **「市場（クラスタ）」の正は `KeywordCluster`**（商材／テーマ単位）。`MarketShare` / `SeasonalityIndex` はここに紐づく。**`TopicCluster` 側の市場規模・シェアは `ClusterMetric.marketVolume` / `.clickShare` を使う**（二重定義を作らない） | D2 |
| **20-4** | **競合ギャップの抽出は `SerpSnapshot.isOurs=false`** を使う。`CompetitorSnapshot` というモデルは存在しない | D1 |
| **20-5** | **`Intervention` の親は `Action` のみ**（`actionId @unique`）。`Experiment` へは Action 経由で辿る。`experimentId` を足さない | D4 |
| **20-7** | **全82モデルが `createdAt` / `updatedAt` を持つ。** 新規モデルを追加する際も必ず付ける。`updatedAt` は `@updatedAt` に任せ、**アプリ側で手動設定しない** | D11 |
| **20-8** | **値の列挙が無い26項目は String のまま。** P1／P1.5 の移行完了後に**実データを集計してから** enum 化する。**移行前に推測で enum を作らない** | D12 |
| **20-9** | **m2 は改修しない。** `Lead` ⇄ m2 Deal の紐付けの正は **MMS 側の `Lead.m2DealId`**。連携時に m2 API が返す DealId を保存する（不可なら `/leads` で手動紐付け） | D13 |
| **20-10** | **広告（P7.5 / P7.6）は §3.4.5 の小額テスト（Step 1）の結果を見てから着手する。** 審査の事前確認は行わない方針のため、**不承認が判明した時点で着手を中止する** | D14 |
| **20-6** | ★**P0 で raw SQL マイグレーションによる部分ユニークインデックスを追加すること。** `MetricSnapshot` の `channelId IS NULL` の行、`AdMetricDaily` の `adGroupId` / `creativeId` が NULL の行は、Postgres の NULL 非同値により通常の `@@unique` が効かない。**これを怠ると §13-④ の冪等キーが機能せず、二重計測を防げない** | N1 |
